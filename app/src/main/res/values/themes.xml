        <resources>

    <!-- ثيم أساسي يعتمد على Material Components -->
    <style name="Theme.Final1"  parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- مثال على إعداد الألوان -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:editTextColor">@color/white</item>
        <item name="android:background">@color/black</item>
        <item name="textInputStyle">@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox</item>
    </style>

    <!-- تنسيق مخصص لرسائل الخطأ -->
    <style name="MyErrorText" parent="TextAppearance.MaterialComponents.Caption">
        <item name="fontFamily">@font/zain_regular</item>
        <item name="android:textColor">@android:color/holo_red_light</item>
    </style>

</resources>
